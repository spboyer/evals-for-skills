export function Footer() {
  return (
    <footer
      className="border-t px-6 py-3 text-center text-sm"
      style={{
        borderColor: "var(--border-default)",
        color: "var(--text-muted)",
      }}
    >
      waza — eval runner for agent skills
    </footer>
  );
}
